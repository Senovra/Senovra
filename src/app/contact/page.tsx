import ContactClient from "./ContactClient";

export const metadata = {
  title: "Contact",
  description:
    "Get in touch with Senovra to discuss your project, idea, or collaboration.",
};

export default function Page() {
  return <ContactClient />;
}